# SPDX-License-Identifier: Apache-2.0
"""Standalone tests; no AReaL, torch or GPU runtime required."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[3]))
